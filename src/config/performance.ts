export const performanceConfig = {
  performanceThresholds: {
    // Percent (%) of JS heap usage considered high
    memoryWarningThreshold: 80,
    // Baseline render time in ms considered acceptable
    renderTime: 16,
    // Acceptable page/screen load time in ms
    pageLoadTime: 2000,
  },
};
export default performanceConfig;
