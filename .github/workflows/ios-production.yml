name: 🚀 iOS ULTRA STABLE V4 (CLOUD)

concurrency:
  group: ios-release-v4
  cancel-in-progress: true

on:
  push:
    branches:
      - prod-release-1170
  workflow_dispatch:
    inputs:
      release_approved:
        description: 'Aprovar build de produção?'
        required: true
        default: 'true'
        type: boolean

jobs:
  ios-cloud-pipeline:
    name: 🍎 iOS Cloud Build & Submit
    runs-on: macos-latest # Voltando para macOS para permitir fallback local se os créditos EAS Cloud acabarem.
    timeout-minutes: 100
    
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      APPLE_ID: ${{ secrets.APPLE_ID }}
      EXPO_ASC_KEY_ID: ${{ secrets.EXPO_ASC_KEY_ID }}
      EXPO_ASC_ISSUER_ID: ${{ secrets.EXPO_ASC_ISSUER_ID }}
      EXPO_ASC_PRIVATE_KEY: ${{ secrets.EXPO_ASC_PRIVATE_KEY }}
      GOOGLE_SERVICE_INFO_PLIST: ${{ secrets.GOOGLE_SERVICE_INFO_PLIST }}
      EXPO_PUBLIC_PROJECT_ID: "6090106b-e327-4744-bce5-9ddb0d037045"
      PROFILE: "production_v13"

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: prod-release-1170

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 🛠️ Install Global EAS CLI
        run: npm install -g eas-cli@latest

      - name: 🛡️ iOS-BUILD-GUARDIAN V4 (Cloud Mode)
        run: |
          chmod +x ./scripts/ci/ios-build-guardian.sh
          ./scripts/ci/ios-build-guardian.sh

      - name: 📄 Upload Build Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-v4
          path: build-logs/
          retention-days: 7
