/**
 * Teste básico para verificar a instalação do Jest
 */

describe('Testes básicos', () => {
  test('Verifica se o ambiente de testes está funcionando', () => {
    expect(1 + 1).toBe(2);
  });
});