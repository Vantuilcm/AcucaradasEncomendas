// Arquivo temporário para resolver erro de importação
// Este arquivo será substituído pela implementação completa posteriormente

export class SecurityService {
  // Métodos básicos para autenticação
  static async validateToken(token) {
    return true; // Implementação temporária
  }

  static async hashPassword(password) {
    return password; // Implementação temporária
  }

  static async comparePasswords(password, hashedPassword) {
    return password === hashedPassword; // Implementação temporária
  }
}